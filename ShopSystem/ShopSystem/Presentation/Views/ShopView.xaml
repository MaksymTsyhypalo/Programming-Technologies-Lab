<Window x:Class="ShopSystem.Presentation.Views.ShopView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Shop" Height="400" Width="700">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <!-- Master: Users List -->
        <GroupBox Header="Users" Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Margin="0,0,10,0">
            <ListBox ItemsSource="{Binding Users}"
                     SelectedItem="{Binding SelectedUser, Mode=TwoWay}"
                     DisplayMemberPath="Name"
                     />
        </GroupBox>

        <!-- Master: Catalog List -->
        <GroupBox Header="Catalog" Grid.Column="1" Grid.Row="0">
            <ListBox ItemsSource="{Binding Catalog}"
                     SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                     DisplayMemberPath="Name"
                     />
        </GroupBox>

        <!-- Detail: Selected Item Details -->
        <GroupBox Header="Item Details" Grid.Column="1" Grid.Row="1">
            <StackPanel Margin="10">
                <TextBlock Text="Name:" FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedItem.Name}" Margin="0,0,0,8"/>
                <TextBlock Text="Price:" FontWeight="Bold"/>
                <TextBlock Text="{Binding SelectedItem.Price}" Margin="0,0,0,8"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
