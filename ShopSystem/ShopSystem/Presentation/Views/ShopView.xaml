<Window x:Class="ShopSystem.Presentation.Views.ShopView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShopSystem.Presentation.Views"
        xmlns:viewmodels="clr-namespace:ShopSystem.Presentation.ViewModels"
        mc:Ignorable="d"
        Title="Shop System" Height="600" Width="900">
    <Window.DataContext>
        <viewmodels:ShopViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Users" Style="{StaticResource HeaderTextStyle}"/>
        <ListBox Grid.Row="1" Grid.Column="0"
                 ItemsSource="{Binding Users}"
                 SelectedItem="{Binding SelectedUser}"
                 DisplayMemberPath="Name"
                 Margin="0,5,5,0"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Catalog Items" Style="{StaticResource HeaderTextStyle}"/>
        <ListBox Grid.Row="3" Grid.Column="0"
                 ItemsSource="{Binding Catalog}"
                 SelectedItem="{Binding SelectedItem}"
                 DisplayMemberPath="Name"
                 Margin="0,5,5,0"/>

        <Grid Grid.Row="0" Grid.RowSpan="4" Grid.Column="1" Margin="5,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Selected Item Details" Style="{StaticResource HeaderTextStyle}"/>
            <StackPanel Grid.Row="1" DataContext="{Binding SelectedItem}" Margin="0,5,0,10">
                <TextBlock Text="{Binding Name, StringFormat='Name: {0}'}" FontWeight="Bold"/>
                <TextBlock Text="{Binding Price, StringFormat='Price: {0:C}'}"/>
                <TextBlock Text="{Binding Id, StringFormat='ID: {0}'}"/>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Quantity: " VerticalAlignment="Center"/>
                <TextBox Width="50" Text="{Binding PurchaseQuantity, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Content="Purchase" Command="{Binding PurchaseCommand}" Margin="10,0,0,0"/>
            </StackPanel>

            <TextBlock Grid.Row="3" Text="Current Inventory" Style="{StaticResource HeaderTextStyle}"/>
            <ListBox Grid.Row="4" ItemsSource="{Binding Inventory}" DisplayMemberPath="DisplayName" Margin="0,5,0,0"/>
            <Button Grid.Row="5" Content="Refresh Inventory" Command="{Binding RefreshInventoryCommand}" Margin="0,10,0,0"/>

        </Grid>
    </Grid>
    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
        </Style>
    </Window.Resources>
</Window>